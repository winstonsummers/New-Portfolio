{
  "name": "phubbell",
  "version": "0.2.0",
  "private": true,
  "homepage": "https://data-wizard.com",
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "ajv": "^6.5.0",
    "gh-pages": "^1.1.0"
  }
}
